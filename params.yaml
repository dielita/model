data:
  repo: git@github.com:team/data-repo.git
  path: data/titanic.dvc
  rev: HEAD                 # можно менять через dvc exp run
  name: titanic

features:
  columns:
  - Pclass
  - Sex_male
  - Age
model:
  type: random_forest
  n_estimators: 300
  max_depth: 9
  random_state: 42
